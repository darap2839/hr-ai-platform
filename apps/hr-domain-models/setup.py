from setuptools import setup, find_packages

setup(
    name="hr_platform",
    version="0.1.0",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
    python_requires=">=3.9",
)
